<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Compose drives the UI; this base theme just keeps the system bars sane before
         the Compose content takes over. -->
    <style name="Theme.RFExplorerMobile" parent="android:Theme.Material.NoActionBar" />
</resources>
